//!3. Write a function to get the first element of an array. (Easy) 
// Explanation: Return array[0], which is the first 
// element (assuming the array is non-empty). 

let arr = [2,4,5,6];

function getTheFirstElem(arr){
    return arr[0];
}
console.log(getTheFirstElem(arr));
