//! Write a recursive function to compute the Nth Fibonacci number. (Medium) 
//Explanation: If N is 0 or 1, return N; otherwise return fib(N-1) + fib(N-2). This recursion follows the Fibonacci definition.

function recursionFibonacci(n){ 
    if(n === 0 || n === 1) return n;
    else{
        
    }

}