//! 4. Write a function to convert a string to lowercase. (Easy) 
// Explanation: Use the toLowerCase() method on the 
// string to convert all characters to lowercase.

let str = "STRING";
function convertstrLowercase(str){
    let lowercase = str.toLowerCase();
    return lowercase;
}
console.log(convertstrLowercase(str));
