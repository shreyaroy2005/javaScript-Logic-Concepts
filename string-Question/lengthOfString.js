//! 1. Write a function that returns the length of a string. (Easy) 
// Explanation: Use the length property (e.g., 
// str.length) to get the number of characters. 

let str = "hello";
function checkLength(str){
    let characters = str.length;
    return characters;
}
console.log(checkLength(str));
